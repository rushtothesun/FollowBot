<UserControl x:Class="FollowBot.Views.Tabs.AurasTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:settingsPath="clr-namespace:FollowBot"
             mc:Ignorable="d"
             d:DataContext="{x:Static settingsPath:FollowBotSettings.Instance}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <GroupBox Grid.Row="0"
                  Grid.Column="0"
                  Grid.ColumnSpan="2"
                  BorderThickness="2"
                  Margin="5,5,10,0"
                  Header="Aura Controls">
            <StackPanel>
                <CheckBox Content="Use Aspect of the Avian"
                          Margin="10,0,10,0"
                          IsChecked="{Binding Path=Auras.EnableAspectsOfTheAvian, Mode=TwoWay}"
                          VerticalAlignment="Center"
                          ToolTip="Should use Aspect of the Avian"></CheckBox>
                <CheckBox Content="Use Aspect of the Cat"
                          Margin="10,0,10,0"
                          IsChecked="{Binding Path=Auras.EnableAspectsOfTheCat, Mode=TwoWay}"
                          VerticalAlignment="Center"
                          ToolTip="Should use Aspect of the Cat"></CheckBox>
                <CheckBox Content="Use Aspect of the Crab"
                          Margin="10,0,10,0"
                          IsChecked="{Binding Path=Auras.EnableAspectsOfTheCrab, Mode=TwoWay}"
                          VerticalAlignment="Center"
                          ToolTip="Should use Aspect of the Crab"></CheckBox>
                <CheckBox Content="Use Aspect of the Spider"
                          Margin="10,0,10,0"
                          IsChecked="{Binding Path=Auras.EnableAspectsOfTheSpider, Mode=TwoWay}"
                          VerticalAlignment="Center"
                          ToolTip="Should use Aspect of the Spider"></CheckBox>
                <CheckBox Content="Use Blasphemy Curses"
                          Margin="10,0,10,0"
                          IsChecked="{Binding Path=Auras.EnableBlasphemyCurses, Mode=TwoWay}"
                          VerticalAlignment="Center"
                          ToolTip="Should cast curses linked with Blasphemy as auras"></CheckBox>
            </StackPanel>

        </GroupBox>

        <Label Grid.Row="1"
               Grid.Column="0"
               Grid.ColumnSpan="2"
               FontWeight="Bold"
               Content="The bot will try to cast all available Auras and Blasphemy Curses automatically."/>
        <Label Grid.Row="2"
               Grid.Column="0"
               Grid.ColumnSpan="2"
               FontWeight="Bold"
               Content="If you use the Flesh and Stone skill, use this button to select your prefered stance."/>
        <Button Grid.Row="3"
                Grid.Column="0"
                FontWeight="Bold"
                Content="Change Stance"
                Click="ChangeStance_OnClick"
                Margin="5,1,25,1"></Button>
        <Border BorderThickness="1"
                BorderBrush="White"
                CornerRadius="10"
                Grid.Row="3"
                Grid.Column="1">
            <Label FontWeight="Bold"
                   Content="{Binding Path=Auras.BloodOrSand}"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"></Label>
        </Border>

    </Grid>

</UserControl>
