<UserControl x:Class="FollowBot.Views.Tabs.OverlayTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:settingsPath="clr-namespace:FollowBot"
             mc:Ignorable="d"
             d:DataContext="{x:Static settingsPath:FollowBotSettings.Instance}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0"
               Grid.Column="0"
               Content="Enable Overlay: "
               Margin="3,5,3,1"/>
        <CheckBox Grid.Row="0"
                  Grid.Column="1"
                  Margin="3"
                  IsChecked="{Binding Overlay.EnableOverlay, Mode=TwoWay}"
                  ToolTipService.ToolTip="Enable and disable the Overlay drawing."/>

        <Label Grid.Row="1"
               Grid.Column="0"
               Content="DrawInBackground: "
               Margin="3,5,3,1"/>
        <CheckBox Grid.Row="1"
                  Grid.Column="1"
                  Margin="3"
                  IsChecked="{Binding Overlay.DrawInBackground, Mode=TwoWay}"
                  ToolTipService.ToolTip="If enabled, the Overlay is draw even when the game is not the top most foreground window."/>

        <Label Grid.Row="2"
               Grid.Column="0"
               Content="Draw Mobs: "
               Margin="3,5,3,1"/>
        <CheckBox Grid.Row="2"
                  Grid.Column="1"
                  Margin="3"
                  IsChecked="{Binding Overlay.DrawMobs, Mode=TwoWay}"
                  ToolTipService.ToolTip="Draw the mobs around you."/>

        <Label Grid.Row="3"
               Grid.Column="0"
               Content="Draw Corpses: "
               Margin="3,5,3,1"/>
        <CheckBox Grid.Row="3"
                  Grid.Column="1"
                  Margin="3"
                  IsChecked="{Binding Overlay.DrawCorpses, Mode=TwoWay}"
                  ToolTipService.ToolTip="Draw the Corpses around you."/>

        <Label Content="Overlay Fps: "
               Grid.Row="4"
               Grid.Column="0"
               Margin="3,5,3,1"/>
        <WrapPanel Grid.Row="4"
                   Grid.Column="1"
                   Margin="3">
            <Slider Name="SlFPS"
                    Minimum="0"
                    Margin="0,5,0,0"
                    Width="160"
                    Value="{Binding Path=Overlay.FPS, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    TickFrequency="1"
                    IsSnapToTickEnabled="True"
                    SmallChange="1"
                    LargeChange="1"
                    Maximum="100"
                    AutoToolTipPrecision="1"
                    VerticalAlignment="Center"
                    ToolTip="Change the Overlay draw frequency, higher number, higher fps and higher resource consumption."/>
            <TextBox Name="TbFPS"
                     Text="{Binding ElementName=SlFPS, Path=Value}"
                     MinWidth="40"
                     HorizontalAlignment="Left"
                     Margin="0,5,0,0"
                     VerticalAlignment="Center"/>
        </WrapPanel>

        <Label Content="Overlay X Position: "
               Grid.Row="5"
               Grid.Column="0"
               Margin="3,5,3,1"/>
        <WrapPanel Grid.Row="5"
                   Grid.Column="1"
                   Margin="3">
            <Slider Name="SlXPos"
                    Minimum="0"
                    Margin="0,5,0,0"
                    Width="160"
                    Value="{Binding Path=Overlay.OverlayXCoord, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    TickFrequency="1"
                    IsSnapToTickEnabled="True"
                    SmallChange="1"
                    LargeChange="1"
                    Maximum="5000"
                    AutoToolTipPrecision="1"
                    VerticalAlignment="Center"
                    ToolTip="Change the Overlay draw X position."/>
            <TextBox Name="TbXPos"
                     Text="{Binding ElementName=SlXPos, Path=Value}"
                     MinWidth="40"
                     HorizontalAlignment="Left"
                     Margin="0,5,0,0"
                     VerticalAlignment="Center"/>
        </WrapPanel>

        <Label Content="Overlay Y Position: "
               Grid.Row="6"
               Grid.Column="0"
               Margin="3,5,3,1"/>
        <WrapPanel Grid.Row="6"
                   Grid.Column="1"
                   Margin="3">
            <Slider Name="SlYPos"
                    Minimum="0"
                    Margin="0,5,0,0"
                    Width="160"
                    Value="{Binding Path=Overlay.OverlayYCoord, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    TickFrequency="1"
                    IsSnapToTickEnabled="True"
                    SmallChange="1"
                    LargeChange="1"
                    Maximum="5000"
                    AutoToolTipPrecision="1"
                    VerticalAlignment="Center"
                    ToolTip="Change the Overlay draw Y position."/>
            <TextBox Name="TbYPos"
                     Text="{Binding ElementName=SlYPos, Path=Value}"
                     MinWidth="40"
                     HorizontalAlignment="Left"
                     Margin="0,5,0,0"
                     VerticalAlignment="Center"/>
        </WrapPanel>

        <Label Content="Overlay Transparency: "
               Grid.Row="7"
               Grid.Column="0"
               Margin="3,5,3,1"/>
        <WrapPanel Grid.Row="7"
                   Grid.Column="1"
                   Margin="3">
            <Slider Name="SlTransparency"
                    Minimum="0"
                    Margin="0,5,0,0"
                    Width="160"
                    Value="{Binding Path=Overlay.OverlayTransparency, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    TickFrequency="1"
                    IsSnapToTickEnabled="True"
                    SmallChange="1"
                    LargeChange="1"
                    Maximum="255"
                    AutoToolTipPrecision="1"
                    VerticalAlignment="Center"
                    ToolTip="Change the Overlay background transparency."/>
            <TextBox Name="TbTransparency"
                     Text="{Binding ElementName=SlTransparency, Path=Value}"
                     MinWidth="40"
                     HorizontalAlignment="Left"
                     Margin="0,5,0,0"
                     VerticalAlignment="Center"/>
        </WrapPanel>
    </Grid>

</UserControl>
